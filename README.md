**MazeProject**

Requirements:
- any python interpreter installed
- PyQt5 installed (Install it via 'pip3 install PyQt5' command)


If you have problems with the pip function, you should check your environment variables
